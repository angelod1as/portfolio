---
import Base from '../../layouts/Base.astro'
import EntryList from '../../components/EntryList.astro'
import LangLinks from '../../components/LangLinks.astro'
import { loadLane } from '../../lib/content'
import { byLang } from '../../lib/entries'

export async function getStaticPaths() {
  return [
    { params: { lang: undefined } },
    { params: { lang: 'en' } },
    { params: { lang: 'pt' } },
  ]
}

const { lang } = Astro.params
const all = await loadLane('essays')
const entries = lang === 'en' || lang === 'pt' ? byLang(all, lang) : all
---

<Base title="Essays" description="Longer pieces, in English and Portuguese.">
  <h1>Essays</h1>
  <LangLinks base="/essays" current={lang} />
  <EntryList entries={entries} />
</Base>
