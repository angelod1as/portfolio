/node_modules
/.pnp
.pnp.js

/coverage

/build

.DS_Store
*.pem

npm-debug.log*
pnpm-debug.log*
pnpm-error.log*

.env.local
.env.development.local
.env.test.local
.env.production.local

.vercel

/dist
/.astro
pnpm-lock.yaml
/public

# vendored tooling — not ours to reformat.
# `.os/` is deliberately NOT here: it holds the gate's own configuration, and a
# gate that excludes its own config from itself is not checking what it claims to.
/.specify
/.serena
/.vscode
/scripts
/.claude
content/**/*.html
